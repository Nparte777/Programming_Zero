#include <bits/stdc++.h>
#include <random.h>

class perceptron{
  float weight[2];
  perceptron(){
    for(int i=0;i<seizeof(weight))
    {
      weight[i]=Use -1+2*((float)rand())/RAND_MAX
    }
  }
}
